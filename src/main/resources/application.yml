routes:
  event-discovery:
    wsEndpoint: https://ws.svil.inps/host/ArcaNotificaEventiWS
  event-details:
    wsEndpoint: https://ws.svil.inps/host/ArcaIntraNewWS/services/ArcaIntraNewWS
  event-finalizer:
    wsEndpoint: https://ws.svil.inps/host/ArcaNotificaEventiWS

ws:
  arcaintra:
    appKey: AB02697
    appName: AB02697
    userId: BATCH
    identityProvider: BATCH
    profilo: 6
    tipoChiave: 01

camel:
  springboot:
    main-run-controller: false
    tracing: false
    backlog-tracing: false
    use-breadcrumb: false

server:
  port: 8080


spring:
  activemq:
    broker-url: amqp://localhost:9876
    user: admin
    password: admin
  jmx:
    enabled: true
  profiles:
    active: test
  datasource:
    url: jdbc:mysql://localhost:3306/db
    driverClassName: com.mysql.jdbc.Driver
    username: user
    password: password
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: false

logging:
  level:
    org.springframework.web: INFO
